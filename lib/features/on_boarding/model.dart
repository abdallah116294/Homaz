class OnBoardingModel {
  String image, title, subTitle;
  int currentPage;

  OnBoardingModel({
    required this.image,
    required this.title,
    required this.subTitle,
    required this.currentPage,
  });
}
